EXPO_PUBLIC_SUPABASE_URL=''
EXPO_PUBLIC_SUPABASE_ANON_KEY=''

EXPO_PUBLIC_AZURE_CLIENT_ID=''
EXPO_PUBLIC_AZURE_TENANT_ID=''

# Public Fitbit client id only. The Fitbit client secret is NOT stored here or in the
# app bundle; it lives as a Supabase secret used by the `fitbit-token` Edge Function
# (set via: supabase secrets set FITBIT_CLIENT_ID=... FITBIT_CLIENT_SECRET=...).
EXPO_PUBLIC_FITBIT_CLIENT_ID=